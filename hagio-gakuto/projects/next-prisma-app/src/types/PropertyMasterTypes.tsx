export type PropertyType = {
  id: number;
  name: string;
};

export type Layout = {
  id: number;
  name: string;
};

export type Feature = {
  id: number;
  name: string;
};

export type ImageCategory = {
  id: number;
  name: string;
};

export type InquiryCategory = {
  id: number;
  name: string;
};
